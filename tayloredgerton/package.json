{
  "name": "tayloredgerton-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run build:site && npm run resume:pdf && npm run linkedin",
    "build:site": "astro build",
    "resume:pdf": "node scripts/generate-resume.mjs",
    "linkedin": "node scripts/generate-linkedin-content.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.1"
  },
  "devDependencies": {
    "puppeteer": "^25.3.0"
  }
}
