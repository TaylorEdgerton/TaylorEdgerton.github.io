{
  "name": "tayloredgerton-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run build:site && npm run resume:pdf",
    "build:site": "astro build",
    "resume:pdf": "node scripts/generate-resume.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.1"
  },
  "devDependencies": {
    "puppeteer": "^25.3.0"
  }
}
